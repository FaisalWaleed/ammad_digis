%h3 Update your password

%p Please enter the new password below and then press "Update Password".

= form_tag admin_password_reset_path, :method => :put do
  = hidden_field_tag :token, @admin.perishable_token
  %p
    = password_field_tag :password
  %p
    = submit_tag "Update Password", :class => 'btn btn-primary'
    = link_to 'Cancel', admin_login_path
